# shellcheck shell=bash
# XDG Bash entrypoint managed by dotfiles.
# Keep the actual configuration in ~/.config/bash/bashrc.
[[ -r "$HOME/.config/bash/bashrc" ]] && source "$HOME/.config/bash/bashrc"
