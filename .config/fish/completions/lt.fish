# Completions for lt (tree list)
complete -c lt -f
complete -c lt -s a -d "Show hidden files"
complete -c lt -s d -d "Show directories only"
complete -c lt -s h -d "Human readable sizes"
complete -c lt -l level -d "Max display depth" -r
